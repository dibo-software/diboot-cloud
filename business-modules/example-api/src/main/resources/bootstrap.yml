server:
  port: 8201
  servlet:
    context-path: /
spring:
  profiles:
    active: dev
  application:
    name: example-api
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
      discovery:
        server-addr: localhost:8848
management:
  endpoints:
    web:
      exposure:
        include: "*"